### To run the frontend. Open cmd to the path for frontend folder.
    1. npm install
    2. npm run dev
### To run the backend. Open cmd to the path for backend folder.
    1. npm install
    2. npm start
### To run the Flask server. Open cmd to the path for flask folder.
    1. python app.py

    
